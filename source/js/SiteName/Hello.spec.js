describe('Hello', () => {
  it('should render correct contents', () => {
    expect(true)
      .to.equal(true)
  })
})

describe('Test factory', function () {
  it('dummy spec to test 2 + 2', function () {
    var result = 2 + 2
    expect(result).toEqual(4)
  })
})
